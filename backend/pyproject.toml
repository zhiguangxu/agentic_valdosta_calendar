[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "openai",
    "requests",
    "beautifulsoup4",
    "python-dateutil",
    "python-dotenv",
]
